# 运行时错误分类与界面映射

## 1. 文档目的

本文档对项目全部错误码进行分类，明确每个错误码的触发条件、严重等级、界面展示位置和用户可执行的恢复动作。

本文档覆盖三个错误域：
- 运行时初始化错误（RuntimeErrorCode）
- 回放错误（ReplayErrorCode）
- Patch 协议错误（PatchErrorCode）

---

## 2. 错误域总览

| 错误域 | 类型定义位置 | 触发阶段 | 结构化载体 |
|--------|------------|---------|-----------|
| 运行时初始化 | `runtime/types.ts` → `RuntimeErrorCode` | `initRuntime()` | `RuntimeErrorInfo` |
| 回放 | `runtime/types.ts` → `ReplayErrorCode` | `replayFrame()` | `ReplayErrorInfo` |
| Patch 协议 | `compiler/ir/patch.ts` → `PatchErrorCode` | `applyPatch()` | `PatchError` |

---

## 3. 运行时初始化错误（RuntimeErrorCode）

### 3.1 错误码清单

| 错误码 | 触发条件 | 严重等级 |
|--------|---------|---------|
| `runtime/webgpu-unavailable` | `navigator.gpu` 不存在 | 致命：环境不支持 |
| `runtime/adapter-unavailable` | `requestAdapter()` 返回 null | 致命：硬件/驱动不可用 |
| `runtime/context-unavailable` | `canvas.getContext('webgpu')` 返回 null | 致命：画布上下文不可用 |
| `runtime/device-request-failed` | `adapter.requestDevice()` 抛异常 | 致命：GPU 设备不可用 |
| `runtime/output-texture-creation-failed` | `createOutputTexture()` 抛异常 | 致命：GPU 资源不足或尺寸非法 |
| `runtime/present-pipeline-creation-failed` | `createPresentPipeline()` 抛异常 | 致命：着色器编译或管线创建失败 |
| `runtime/unknown` | 未分类的异常 | 致命：未知原因 |

### 3.2 界面展示映射

所有运行时初始化错误统一通过以下路径展示：

| 展示位置 | 字段 | 展示内容 |
|---------|------|---------|
| CanvasWorkspace 底部错误栏 | `store.error` | 错误消息字符串 |
| InspectorPanel → 运行时状态 → 状态 | `store.status` | 显示 `error` |
| InspectorPanel → 运行时状态 → 回放错误 | `store.replayError` | 仅在回放时触发时显示 |
| Store 内部结构化字段 | `store.runtimeError` | `{ code, message }` |

### 3.3 用户恢复动作建议

| 错误码 | 用户可执行动作 |
|--------|--------------|
| `runtime/webgpu-unavailable` | 更换支持 WebGPU 的浏览器（Chrome/Edge 113+） |
| `runtime/adapter-unavailable` | 检查显卡驱动是否最新，或更换硬件 |
| `runtime/context-unavailable` | 刷新页面，检查画布 DOM 是否被正确挂载 |
| `runtime/device-request-failed` | 检查 GPU 是否被其他程序独占，尝试重启浏览器 |
| `runtime/output-texture-creation-failed` | 检查画布尺寸是否过大，或 GPU 显存是否不足 |
| `runtime/present-pipeline-creation-failed` | 检查浏览器 WebGPU 着色器支持情况 |
| `runtime/unknown` | 查看控制台完整错误堆栈，联系开发者 |

---

## 4. 回放错误（ReplayErrorCode）

### 4.1 错误码清单

| 错误码 | 触发条件 | 严重等级 |
|--------|---------|---------|
| `replay/runtime-unavailable` | `runtime.value === null` 时调用 `replayFrame` | 可恢复：运行时未初始化 |
| `replay/missing-data` | 帧记录不存在，或帧记录缺少 `artifact` / `compileContextSnapshot` | 可恢复：数据不完整 |
| `replay/incompatible-artifact-version` | 帧记录的 `artifactSchemaVersion` 与当前版本不一致 | 可恢复：版本不兼容 |
| `replay/signature-mismatch` | 回放时渲染签名与历史帧记录的签名不一致 | 警告：渲染结果可能不一致 |

### 4.2 界面展示映射

| 展示位置 | 字段 | 展示内容 |
|---------|------|---------|
| InspectorPanel → 运行时状态 → 回放状态 | `store.replayStatus` | `idle` / `success` / `error` |
| InspectorPanel → 运行时状态 → 回放错误 | `store.replayError` | 错误消息字符串 |
| Store 内部结构化字段 | `store.replayErrorInfo` | `{ code, message }` |
| 帧数据面板 → 一致性校验 | `payload.renderVerificationSnapshot` | `一致` / `不一致` / `未校验` |

### 4.3 用户恢复动作建议

| 错误码 | 用户可执行动作 |
|--------|--------------|
| `replay/runtime-unavailable` | 等待运行时初始化完成后再回放 |
| `replay/missing-data` | 该帧数据不完整，无法回放，尝试选择其他帧 |
| `replay/incompatible-artifact-version` | 该帧由旧版本生成，需重新渲染当前帧 |
| `replay/signature-mismatch` | 回放结果与历史记录不一致，可能运行环境已变化 |

---

## 5. Patch 协议错误（PatchErrorCode）

### 5.1 错误码清单

| 错误码 | 触发条件 | 严重等级 |
|--------|---------|---------|
| `IR_PATCH_VIOLATION` | 通用 patch 结构违规 | 可恢复：patch 被拒绝 |
| `IR_STATIC_BOUNDARY_VIOLATION` | 违反静态边界硬约束（time/frame/animated 等禁用字段） | 可恢复：patch 被拒绝 |
| `IR_PATCH_TARGET_NOT_FOUND` | targetId 在 IR 中不存在 | 可恢复：patch 被拒绝 |
| `IR_PATCH_DUPLICATE_ID` | add 时 id 已存在 | 可恢复：patch 被拒绝 |
| `IR_PATCH_DANGLING_REF` | remove 时仍有引用 | 可恢复：patch 被拒绝 |
| `IR_PATCH_SCHEMA_MISMATCH` | params 不匹配 opcode schema | 可恢复：patch 被拒绝 |
| `IR_PATCH_PATH_NOT_ALLOWED` | paramKey 不在白名单 | 可恢复：patch 被拒绝 |
| `IR_PATCH_INVALID_VALUE` | 数值越界（radius < 0 / NaN 等） | 可恢复：patch 被拒绝 |
| `IR_PATCH_ATOMIC_INCOMPLETE` | AtomicTopologyPatch 缺字段 | 可恢复：patch 被拒绝 |
| `IR_PATCH_BATCH_NESTED` | PatchBatch 嵌套 PatchBatch | 可恢复：patch 被拒绝 |
| `IR_PATCH_TRANSACTION_CONFLICT` | 原子 patch 与普通 patch 冲突 | 可恢复：patch 被拒绝 |

### 5.2 界面展示映射

Patch 错误当前不直接在界面面板展示，而是通过以下路径传播：

| 展示位置 | 字段 | 展示内容 |
|---------|------|---------|
| CanvasWorkspace 底部错误栏 | `store.error` | 错误消息字符串（applyDemoPatch 失败时写入） |
| 帧记录 | `record.error` | 错误消息写入帧记录的 error 字段 |
| 帧记录 | `record.status` | 帧状态标记为 `error` |

### 5.3 用户恢复动作建议

所有 Patch 错误统一建议：检查 patch 参数合法性，修正后重新提交。

---

## 6. 错误码与界面展示映射策略

### 6.1 展示原则

1. **致命错误优先**：运行时初始化错误（fatal）优先于回放错误和 patch 错误展示
2. **结构化优先**：store 内部同时保存结构化字段（`runtimeError` / `replayErrorInfo`）和字符串字段（`error` / `replayError`），界面层优先消费字符串，诊断层优先消费结构化
3. **不覆盖原则**：回放错误不覆盖运行时初始化错误；运行时初始化错误存在时，回放操作直接返回 `replay/runtime-unavailable`
4. **帧记录完整**：无论成功还是失败，每次渲染和回放都写入帧记录，失败帧记录 `status = 'error'` 并填充 `error` 字段

### 6.2 界面展示分工

| 界面区域 | 负责展示的错误类型 | 数据来源 |
|---------|------------------|---------|
| CanvasWorkspace 状态指示器 | 运行时状态（idle/initializing/ready/error） | `store.status` |
| CanvasWorkspace 底部错误栏 | 当前运行时错误或 patch 错误 | `store.error` |
| InspectorPanel → 运行时状态 | 运行时状态、画布尺寸、输出格式、回放状态、回放错误 | `store.status` / `store.replayStatus` / `store.replayError` |
| InspectorPanel → 补丁调试 | 当前场景、图层、指令、补丁编号、补丁摘要 | `store.currentScenario` / `store.currentLayerId` 等 |
| InspectorPanel → 设备能力 | 存储格式、纹理尺寸限制、计算限制 | `store.capability` |
| 帧数据面板 → 帧摘要 | 帧编号、时间戳、耗时、状态、补丁摘要、工件版本 | `FrameSnapshot` |
| 帧数据面板 → 一致性校验 | 渲染签名状态、签名说明 | `FrameSnapshot.renderVerificationState` |

### 6.3 错误码到用户消息映射

运行时初始化错误和回放错误的消息已在 `device.ts` 和 `runtime.ts` 中硬编码，与错误码一一对应。界面层直接展示 `message` 字段，不做二次翻译。

Patch 错误的消息在 `patch.ts` 的 `PatchError` 构造函数中生成，格式为 `Patch validation failed: {首个违规描述}`。

---

## 7. 错误码版本管理

### 7.1 新增错误码

新增错误码时必须：
1. 在类型定义中追加（`RuntimeErrorCode` / `ReplayErrorCode` / `PatchErrorCode`）
2. 在本文档对应章节补充清单
3. 在触发代码中用 `createRuntimeError()` 或 `new PatchError()` 包装
4. 补充对应测试用例

### 7.2 废弃错误码

废弃错误码时必须：
1. 保留类型定义（不删除，标注 `@deprecated`）
2. 在本文档标注废弃原因和替代码
3. 确保已有测试仍通过

### 7.3 当前版本

- RuntimeErrorCode: 7 个码（含 `runtime/unknown` 兜底）
- ReplayErrorCode: 4 个码
- PatchErrorCode: 11 个码

---

## 8. 与代码的对应关系

| 文档章节 | 代码位置 | 说明 |
|---------|---------|------|
| §3 运行时初始化错误 | `runtime/types.ts` → `RuntimeErrorCode` | 类型定义 |
| §3 运行时初始化错误 | `runtime/device.ts` → `createRuntimeError()` | 错误创建 |
| §3 运行时初始化错误 | `stores/runtime.ts` → `normalizeRuntimeError()` | 错误归一化 |
| §4 回放错误 | `runtime/types.ts` → `ReplayErrorCode` | 类型定义 |
| §4 回放错误 | `stores/runtime.ts` → `replayFrame()` | 错误触发 |
| §5 Patch 协议错误 | `compiler/ir/patch.ts` → `PatchErrorCode` | 类型定义 |
| §5 Patch 协议错误 | `compiler/ir/patch.ts` → `PatchError` | 错误类 |
| §5 Patch 协议错误 | `compiler/ir/patchEngine.ts` → `applyPatch()` | 错误触发 |
| §6 界面展示映射 | `stores/runtime-inspector.ts` → `createInspectorSections()` | 检查面板投影 |
| §6 界面展示映射 | `components/ui/CanvasWorkspace.vue` | 画布工作区错误栏 |
| §6 界面展示映射 | `services/frame/adapter.ts` → `createFrameSections()` | 帧数据面板 |
