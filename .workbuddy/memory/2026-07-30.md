# 2026-07-30

## PixelForge 项目整体评估

用户要求评价项目。完成了一次文档 + 代码 + 实测三维度的全面评估。

### 关键事实(实测验证)
- 文档声称:3338 项测试全部通过(README/项目链路总览/阶段六完成定义均如此写)
- 最新提交 6959241 声称:3438 项测试通过 + TS 0 错误
- 实测(vitest run):2859 项测试,12 项失败,21 项跳过,2826 通过
- 实测(vue-tsc --noEmit):0 错误 ✅(与声称一致)
- 失败测试:engine.test.ts 7 个(play/toggle/timeline 步进/patches 链路)、frameScheduler.test.ts 3 个、input.test.ts 1 个(mapRange 零范围)、serializer.test.ts 1 个(深拷贝)
- 失败与未提交改动无关,是 HEAD 提交本身引入的(播放引擎 play() 不生效导致级联失败)

### 项目结构要点
- 最新提交(6959241,今天)删除了整个 ProTimeline UI 组件目录(约 20 个 .vue,含 1046 行主组件)+ editor/timeline,共删 131 文件 3.4 万行,替换为新 UI(TopHeader/LeftRail/TimelinePanel/CanvasViewport 等)
- 但底层逻辑保留:world/wdl、editor/asset-genome、editor/audio、editor/effects 等仍在
- 文档(项目链路总览/阶段六完成定义)仍大量描述已删除的 ProTimeline 组件,文档已落后于代码
- 无 CI(workflows)、无 ESLint/Prettier 配置
- 残留脚手架:components/HelloWorld.vue(Initial commit 遗留,无引用)
- 工作区有未提交改动:App.vue、TimelinePanel.vue 等 10 个文件 + 2 个未跟踪 HTML

### 评估结论(给用户)
优势:功能广度极大、架构分层清晰(FrameSnapshot 统一消费/错误归一化)、文档纪律好、代码质量高(纯函数+DI、仅 1 处 any、无 TODO)、真实 LLM 集成
风险:文档/代码一致性差(测试基线失真)、无 CI/Lint 工程化短板、单作者大规模重构风险

---

## 按评估建议执行修复(用户指示"按你说得来")

### 任务 1:修复 12 个失败测试 ✅
根因:HEAD(6959241)删除 @/stores/timeline、@/animation/drivers/inputDriver、@/editor/timeline/player 后,测试本地 stub 是残次品(setPlaying 不改状态/每次返回新对象/update 恒返回 0/mapRange 丢零范围守卫/serializer mock tracks 为空)。
修复:
- engine.ts / engineScheduled.ts:EngineDeps 增加可选 applyFrameToRuntime 注入(对齐项目 DI 风格,默认内部存根)
- engine.test.ts / frameScheduler.test.ts:timeline stub 改为有状态共享单例 fake(setPlaying/seek 真实改状态,fps=60 对齐原 store),engine.test.ts 的 InputDriver stub 改为真实求值 fake(绑定+信号映射+patch 计数)
- input.test.ts:mapRange 补零范围守卫(inMax===inMin → outMin)
- serializer.test.ts:mock timeline 补一条非空 track
验证:全量 2859 项(2838 通过 + 21 跳过,0 失败);vue-tsc 0 错误
注:engine 系列修复已被用户并行提交 af0ed5f + 8a522b2 收录

### 任务 2:CI ✅
新建 .github/workflows/ci.yml(仓库根在桌面 PixelForge/,working-directory=PixelForge):node22 + npm ci + vue-tsc --noEmit + vitest run,push/PR 触发

### 任务 3:文档同步 ✅
修正 5 份文档测试基线(3338→2859 实测值,vue-tsc 7 预存错误→0 错误):项目链路总览/阶段六完成定义/项目阶段划分/文档索引/PixelForge/README;链路总览目录职责更新(src/animation 已删、src/storage 三层存储新增、pro-timeline 删除注明)

### 任务 4:清理 ✅
git rm HelloWorld.vue(脚手架残留);app.ts 删 [Generate] console.log 调试残留

### ⚠️ 重要:工作区并行重构
修复期间用户(或另一会话)在并行大改:af0ed5f 提交新增 5 个面板组件+收录我的修复,随后工作区又出现 67 个文件的未暂存删除(全部 src/components、authoring/image、authoring/llm)。2859 全通过的快照取自并行删除开始前;world/director 仍引用被删的 authoring/llm,重构落地后测试数会再变。未提交我的剩余改动以免干扰其暂存区。
