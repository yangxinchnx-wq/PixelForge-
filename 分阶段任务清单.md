# 分阶段任务清单

## 1. 文档目的

本文档把整个项目按阶段拆成可执行的小清单。

---

# 阶段一：图形运行链路可行性验证

## 当前状态
- 已完成
- 已封板

---

# 阶段二：运行时稳定性与历史态一致性

## 当前状态
- 已完成
- 已封板

## 已完成项摘要
- [x] 运行时错误第一版分层
- [x] 回放错误第一版分层
- [x] WebGPU 初始化失败测试
- [x] 历史态正式投影模块化
- [x] 检查面板历史态投影复查
- [x] 渲染签名一致性验证
- [x] 结果级一致性进入帧数据面板
- [x] 阶段二专门文档补齐

---

# 阶段三：编辑器工作流与时间轴增强

## 当前状态
- 已完成
- 已封板

## 已完成项摘要
- [x] 时间轴缩放（1x-10x，Ctrl+滚轮）
- [x] 时间轴平移（滚动容器 + 滚轮水平滚动）
- [x] 区间选择（标尺拖拽 + Shift+点击扩展 + 高亮覆盖层）
- [x] 关键帧显示策略（菱形标记 + 状态着色 + 缩放级别标签切换）
- [x] 多轨道组织规则（补丁轨道→关键帧轨道→验证轨道）
- [x] 编辑帧与展示帧关系区分
- [x] 回放状态指示器
- [x] 工具栏分组（播放组/缩放组/信息组）
- [x] 摘要数据与技术数据分离
- [x] 多种导出格式策略（快照/调试/CSV/完整）

---

# 阶段四：多图层 / 多区域 / 效果系统扩展

## 当前状态
- 已完成
- 已封板

## 已完成项摘要
- [x] Render IR V2 扩展支持多图层、区域边界、静态效果
- [x] WGSL 多图层循环求值与 6 种混合模式（Normal/Multiply/Screen/Overlay/Add/Subtract）
- [x] 独立效果后处理 Pass（effect_post.wgsl）
- [x] 三层 Cache Key 细化（StaticKey 含 blendModes / StructuralKey / DynamicKey）
- [x] 工件迁移 descriptorBuffer V1→V2（长度为 2*layerCount）
- [x] Uniforms 结构调整（resolution/seed/layerCount）
- [x] 203 项自动化测试全部通过
- [x] 文档对照审计完成（骨架 §3.3/§4.5/§4.6.3 对齐）

---

# 阶段五：产品化与长期演进能力

## 当前状态
- 已完成并可封板

## 任务清单

### 5.1 持久化仓储
- [x] FrameRepository 从内存实现升级为 IndexedDB 持久化
- [x] 支持多会话场景下的帧记录恢复
- [x] 持久化数据格式版本化（支持未来 schema 迁移）
- [x] 持久化层自动化测试

### 5.2 错误诊断体系完善
- [x] runtime 层错误码枚举化（替代字符串错误）
- [x] GPU 运行时错误分类（shader 编译错误/执行错误/资源错误）
- [x] 导出错误分类
- [x] 错误诊断面板（可视化错误链路）
- [x] 错误诊断层自动化测试

### 5.3 高等级一致性验证
- [x] 像素级回放一致性验证（历史帧画面一致性证明）
- [x] 多图层混合一致性验证
- [x] 一致性验证结果进入帧数据面板
- [x] 一致性验证自动化测试

### 5.4 性能指标采集制度化
- [x] GPU profiler 实现（dispatch time / texture write time / present time 分离）
- [x] CPU 指标采集（requirement clarify / parser / IR build / descriptor encode time）
- [x] 内存指标采集（descriptor buffer / auxiliary buffer / texture memory bytes）
- [x] 性能面板 UI（可视化指标展示）
- [x] 性能指标自动化测试

### 5.5 阶段五收口标准
- [x] 帧记录可在页面刷新后恢复
- [x] 运行时错误有明确结构化分类
- [x] 回放一致性可被客观验证
- [x] 性能指标可被采集和展示
- [x] 所有新增功能有自动化测试覆盖

---

## 10. 当前建议执行顺序

从现在开始，建议这样推进：

1. 阶段一~四不再追加任务
2. 当前进入阶段五
3. 阶段五内部按 5.1→5.2→5.3→5.4 顺序推进
4. 阶段五未收口前，不进入 Phase F（Timeline/Revision）

---

# 阶段六：可视化编程引擎 + 实时动画 + 专业时间轴 + 渲染导出

## 当前状态
- 进行中（Step 25-40.2 已完成,Step 40.3+ 待规划）

## 已完成项摘要

### 6.1 可视化编程引擎 + Material/Shader 系统（Step 25-28）
- [x] Step 25：RenderIR Graph Editor（节点化编辑,DAG 图结构）
- [x] Step 26：Graph Runtime（拓扑排序 + 内容寻址缓存 + 纹理池 + 资源管理）
- [x] Step 27：Graph Editor 高级交互（无限画布 + 节点拖拽/连线 + 小地图 + 快捷键）
- [x] Step 28：Material/Shader Node System（WGSL 自动生成 + Shader Cache + GPU 核融合）

### 6.2 实时动画 + 输入系统（Step 29-30）
- [x] Step 29：Advanced Timeline + Animation Engine（关键帧 + 曲线 + ParamPatch + GPU Uniform 更新）
- [x] Step 30：Audio Reactive + Sensor Input（音频/麦克风/MIDI/摄像头驱动视觉反馈）
- [x] 动画执行链路打通：Timeline → evaluateTrack → ParamPatch → GraphNode/MaterialNode → compiler → WGSL + UniformBuffer → WebGPU → Canvas

### 6.3 专业时间轴 Pro Timeline（Step 31.1-31.9）
- [x] Step 31.1：Timeline Core（Project/Sequence/Track/Clip 数据模型 + Command 模式 + Snap 吸附,118 测试）
- [x] Step 31.2：Clip CRUD UI（ProTimeline 完整 UI + 拖拽 + 右键菜单,30 测试）
- [x] Step 31.3：多轨道编辑增强（重命名/着色/删除/复制/重排序）
- [x] Step 31.4：高级片段编辑（剪切/分割/波纹删除/多选）
- [x] Step 31.5：Frame Scheduler（60FPS 时间预算 + 5 阶段调度链 + GPU 资源生命周期,88 测试）
- [x] Step 31.6：多 Sequence 切换 + 嵌套 Sequence（Project/SequenceCommands/NestedSequenceResolver,67 测试）
- [x] Step 31.7：跨 Sequence 标尺对齐 + 拖拽片段（SequenceAlignment/CrossSequenceCommands,54 测试）
- [x] Step 31.8：嵌套 Sequence 双击进入子 Sequence 编辑模式（面包屑导航,35 测试）
- [x] Step 31.9：Sequence 模板/预设库（6 内置 + localStorage 自定义,51 测试）

### 6.4 渲染导出模块（Step 32）
- [x] RenderConfig（配置 + 6 预设 + 验证 + 帧序列计算）
- [x] RenderPipeline（状态机 + 逐帧调度 + 暂停/取消/失败处理）
- [x] RenderStore（Pinia Store + 进度 computed + 控制 actions）
- [x] ProTimelineRenderPanel（配置编辑 UI + 预设 + 进度条 + 输出列表,46 测试）

### 6.5 视频效果链 Effect Chain（Step 34）
- [x] effectChain（17 种效果类型 × 5 大类:色彩/模糊/变换/风格化/合成）
- [x] 纯函数不可变操作（append/insert/remove/move/enable/updateParams/rename/collapse）
- [x] 5 个内置预设（电影感/复古/梦幻/故障/锐利高清）
- [x] effectChainStore（Pinia Store 管理 clipId→EffectChain 映射,Map 响应式触发）
- [x] ProTimelineEffectChain（UI:与 selectionStore 联动,按大类分组的添加菜单,效果列表 + 参数编辑器）
- [x] 77 测试通过（EC 11 + CH 17 + Q 6 + V 9 + P 9 + M 7 + S 18）

### 6.6 阶段六收口标准
- [x] 2825 项自动化测试全部通过（对比阶段五封板时 258 项,增长 11.0 倍）
- [x] vue-tsc 零 TS 错误（基线归零）
- [x] 可视化编程引擎、动画系统、输入系统、专业时间轴、渲染导出、音频混音、视频效果链、Asset Genome、AI Director、WDL 声明式渲染 DSL 全链路打通

## 关键代码位置

| 模块 | 目录 |
|------|------|
| Graph 引擎 | `src/graph/` |
| Material/Shader | `src/material/` |
| Animation | `src/animation/` |
| Input 系统 | `src/input/` |
| Pro Timeline UI | `src/components/editor/pro-timeline/` |
| Timeline Core | `src/editor/timeline/` |
| Render 导出 | `src/editor/render/` |
| Audio 混音器 | `src/editor/audio/` |
| Effect Chain | `src/editor/effects/` |
| Asset Genome | `src/editor/asset-genome/` |
| Frame Scheduler | `src/runtime/frameScheduler.ts` |
| Engine Scheduled | `src/runtime/engineScheduled.ts` |
| GPU 资源管理 | `src/runtime/gpuResourceManager.ts` |

## Git 提交记录

| Commit | 内容 |
|--------|------|
| `5fde201` | Initial commit |
| `590ad87` | Remove node_modules and target, add gitignore |
| `96da685` | feat: implement visual programming engine + timeline core (Step 25-31.1) |
| `8e9651e` | feat: Step 31.1-31.5 Timeline Core + Clip Engine + UI + Video Decode + Compositor |
| `fbfcda6` | feat: Step 31.5+/31.6-32 Frame Scheduler + Multi-Sequence + Nested Editing + Templates + Render Export |
| `127d416` | feat: Step 33 音频混音器模块 |
| `93a3959` | fix: Step 33 自检修复 |
| `5f39c93` | feat: Step 34 视频效果链 Effect Chain - 17种效果/5大类/预设/Store/UI/77测试 |
| `1361152` | feat: Step 35.1 Asset Genome Asset Registry Core - 14种AssetKind/4大类/CRUD/查询/验证/Store/75测试 |
| `d9d9721` | feat: Step 35.2-35.7 Asset Genome 后续路线 - Reference Graph/Impact Analysis/Content Hash/Lazy Loader/Browser UI/Packaging |
| `163b3f9` | feat: Step 36.1-36.6 AI Director - Context Engine/Enhanced Intent/Conversation/Timeline/UI (63 tests, 2504 total) |
| `43404ae` | feat: Step 37.1-37.6 WDL 声明式渲染 DSL - Lexer/Parser/Compiler/Validator/Editor UI (141 tests, 2645 total) |
| `96ae728` | feat: Step 38.1 Monaco Editor 集成 + WDL 语法高亮 (33 tests, 2678 total) |
| `264d279` | feat: Step 38.2 WDL 自动补全 (34 tests, 2712 total) |
| `754c3fd` | feat: Step 38.3 Validator 错误内联提示 (25 tests, 2737 total) |
| `bf6e9ff` | feat: Step 38.4 WDL ↔ Graph 双向同步 (25 tests, 2762 total) |
| `b3a69c9` | feat: Step 38.5 WDL 模板库 (29 tests, 2791 total) |
| `8a1afaa` | feat: Step 38.6 WDL ↔ ProTimeline 绑定 (34 tests, 2825 total) |
| `ec44418` | feat: Step 39.1 渲染性能 Profiler (45 tests, 2870 total) |
| `327b48c` | feat: Step 39.2 GPU 资源池化 BufferPool (56 tests, 2926 total) |
| `4151207` | feat: Step 39.3 多 Pass 渲染管线(离屏 RenderTarget + 后处理链) |
| `2667e83` | feat: Step 39.4 渲染签名缓存(三级缓存命中率) |
| `d37b93d` | feat: Step 40.1 设置面板(主题切换 + 用户偏好 + 持久化) |
| (本次提交) | feat: Step 40.2 快捷键体系(CommandRegistry + CommandPalette + useCommandShortcuts) |

## 已完成（Step 33）

- [x] 音频混音器（Audio Mixer）— Step 33
  - [x] 混音核心：dB/linear 转换、等功率声像、mute/solo 优先级逻辑
  - [x] 效果链系统：EQ / 压缩器 / 混响 / 增益，支持启用/禁用/重排序
  - [x] Pinia Store + Web Audio API 集成（AudioContext / AnalyserNode / 电平表）
  - [x] UI 面板：轨道列（声像旋钮/音量推子/M/S/效果）+ 主输出 + 电平表
  - [x] 78 测试通过（AM 21 + AE 13 + AC 10 + AQ 10 + AV 6 + AS 18）
  - [x] 自检修复：移除未使用代码 + RAF 驱动电平表 + 补充边界测试

## 已完成（Step 34）

- [x] 视频效果链（Effect Chain）— Step 34
  - [x] 17 种效果类型 × 5 大类（色彩/模糊/变换/风格化/合成）
  - [x] 纯函数不可变操作 + 参数验证（clamp 越界值）+ 结构完整性验证
  - [x] 5 个内置预设：电影感 / 复古 / 梦幻 / 故障 / 锐利高清
  - [x] Pinia Store 管理 clipId→EffectChain 映射,Map 响应式触发
  - [x] UI 面板：与 selectionStore 联动,添加效果下拉菜单（按大类分组）+ 效果列表 + 参数编辑器（按类型分支）
  - [x] 77 测试通过（EC 11 + CH 17 + Q 6 + V 9 + P 9 + M 7 + S 18）

## 已完成（Step 35.1）

- [x] Asset Genome 基础 — Asset Registry Core — Step 35.1
  - [x] 14 种 AssetKind × 4 大 AssetCategory 类型系统（media/shader/scene/config）
  - [x] AssetMetadata + AssetRecord 数据模型（id/kind/category/name/source/version/tags/...）
  - [x] 注册表纯函数 CRUD（register/unregister/rename/update/addTag/removeTag/bumpVersion/clear）
  - [x] 查询纯函数（byId/byKind/byCategory/byTag/bySource/search/group）
  - [x] 验证纯函数（validateAssetRecord 8 种错误码 + validateRegistry）
  - [x] 旧版 Asset 兼容转换（assetFromLegacy）
  - [x] Pinia Store + 响应式 Map 触发模式 + computed 查询
  - [x] 75 测试通过（T 8 + F 5 + R 18 + Q 15 + V 9 + L 2 + S 18）

## 已完成（Step 35.2-35.7 Asset Genome 后续路线）

- [x] Step 35.2: Asset Reference Graph — 依赖关系 DAG + 引用追踪 + Store + 38 测试
  - [x] ReferenceGraph 双向邻接表（adjacency + reverseIndex）,支持 uses/extends/embeds 三种引用类型
  - [x] 工厂: createReferenceGraph / createReference(自引用抛错)
  - [x] CRUD: addReference(同 source+target+type 去重) / removeReference / removeAllReferencesForAsset / clearReferenceGraph
  - [x] 查询: getReferences / getReferencers / hasReference / getAllReferences / getReferenceCount / getReferencesByType / getOutDegree / getInDegree
  - [x] Pinia Store(useReferenceGraphStore)+ 响应式 Map 触发 + computed 查询
  - [x] 38 测试通过(F 6 + C 10 + Q 11 + S 11)

- [x] Step 35.3: Impact Analysis — 影响分析 + 循环检测 + 下游查询 + 18 测试
  - [x] getDownstreamImpact: BFS 沿 reverseIndex 传播,返回受影响资产 ID 集合
  - [x] getUpstreamDependencies: BFS 沿 adjacency 传播
  - [x] getDownstreamDepth / getDownstreamImpactDetails: 返回 ImpactItem[](assetId + depth + path)
  - [x] hasCycle: DFS 三色标记法(白/灰/黑),遇灰节点=有环
  - [x] findCycles: DFS + path 追踪,返回环路路径列表
  - [x] topologicalSort: Kahn 算法(入度),有环返回 null
  - [x] 18 测试通过(IA 10 + IAC 5 + IAD 3)

- [x] Step 35.4: Content Hash & Dedup — 内容哈希 + 去重 + 相似检测 + 25 测试
  - [x] fnv1a32: FNV-1a 32-bit 快速哈希(纯函数)
  - [x] computeContentHash: 基于 kind+name+size+payloadRef+tags 计算内容指纹
  - [x] computeBinaryHash: SHA-256(SubtleCrypto)降级 FNV-1a
  - [x] buildHashIndex / findDuplicates / findDuplicatesOf: 基于 contentHash 去重检测
  - [x] computeSimilarity: Jaccard 名称(0.4) + Jaccard 标签(0.4) + 种类(0.2)
  - [x] findSimilarPairs / findSimilarTo: 全量 / 指定资产相似检测
  - [x] generateDedupSuggestions: 保留最新创建的(createdAt 降序第一个),返回 DedupSuggestion[]
  - [x] 25 测试通过(CH 9 + CD 6 + CS 7 + CDS 3)

- [x] Step 35.5: Lazy Loading & Index — 加载状态管理 + 多维索引 + 懒加载协调器 + 28 测试
  - [x] LoadState 状态机(unloaded / loading / loaded / error)
  - [x] LoadStatusTable + 状态函数: markLoading / markLoaded / markError / markUnloaded / getPendingLoads(按优先级) / getLoadStats
  - [x] AssetIndex 多维倒排索引(nameIndex / kindIndex / categoryIndex / tagIndex)
  - [x] 索引查询: queryByName(大小写不敏感) / queryByKind / queryByCategory / queryByTags(OR 语义)
  - [x] LazyLoader 协调器: requestLoad(标记 unloaded 入队) / executeLoad(loading → loaded/error) / executeBatchLoads(顺序执行避免状态合并冲突)
  - [x] 28 测试通过(LS 11 + AI 10 + LL 7)

- [x] Step 35.6: Asset Browser UI — 资产浏览器面板 + 集成到 ProTimeline
  - [x] ProTimelineAssetBrowser.vue: 模态弹窗设计
  - [x] 搜索框(名称/标签/描述,大小写不敏感)+ 大类筛选下拉
  - [x] 标签栏(前 10 个标签,点击搜索)
  - [x] 资产列表按大类分组(media / shader / scene / config)
  - [x] 详情面板:ID/种类/大类/来源/版本/大小/创建时间/更新时间 + 标签 + 引用数(出边/入边)+ 描述
  - [x] 使用 useAssetRegistryStore + useReferenceGraphStore
  - [x] --pf-* 设计令牌 + cubic-bezier(0.22, 1, 0.36, 1) 180ms 过渡
  - [x] 集成到 ProTimeline.vue

- [x] Step 35.7: Asset Packaging — 打包导出 + 导入 + 18 测试
  - [x] AssetPackage 接口: formatVersion=1 + name + createdAt + assets[] + references[]
  - [x] createPackage / serializePackage(JSON) / deserializePackage(JSON) + 格式校验
  - [x] validatePackage: 包名非空 + 引用 ID 存在性 + 资产 ID 唯一性
  - [x] mergePackage: 已存在 ID 跳过,返回新集合 + ImportResult
  - [x] createPackageWithIds: 过滤导出(只保留 source+target 都在列表中的引用)
  - [x] 18 测试通过(AP01-AP18)

## 已完成（Step 36.1-36.6 AI Director）

- [x] Step 36.1: Director Context Engine — 上下文序列化 + 18 测试
  - [x] DirectorContext 接口: summary / canvasSize / layerCount / effectCount / hasTimeline / modifiableParams
  - [x] ModifiableParam 接口: targetEntity / targetId / paramKey / currentValue / description
  - [x] 参数描述映射: OPCODE_DESCRIPTION / LAYER_PARAM_DESCRIPTION / EFFECT_PARAM_DESCRIPTION
  - [x] buildDirectorContext / buildCreateModeContext / buildModifyModeContext 三模式上下文构建
  - [x] serializeLayer 输出格式: `[0] id=layer_01 SOLID_COLOR(纯色填充) color=[1,0,0,1] blendMode=normal`
  - [x] 18 测试通过(DC01-DC13 + CC01-CC02 + MC01-MC03)

- [x] Step 36.2: Enhanced Intent + System Prompt — 增强意图解析 + 上下文感知 + 24 测试
  - [x] DirectorMode = 'create' | 'modify' | 'animate' | 'analyze' 四种操作模式
  - [x] EnhancedIntent extends DirectorIntent: 添加 mode / keywords / referencedLayerIds
  - [x] 关键词数组: CREATE_KEYWORDS / MODIFY_KEYWORDS / ANIMATE_KEYWORDS / ANALYZE_KEYWORDS(中英文)
  - [x] parseEnhancedIntent: 模式检测 + 关键词提取 + 图层引用检测(直接 ID + 序数引用"第一个图层")
  - [x] parseChineseNumber: 解析中文数字(一/二/三/...)
  - [x] buildContextAwareSystemPrompt: 按模式注入不同上下文(create/modify/animate/analyze)
  - [x] decideWithContext: 增强版决策(注入上下文到系统提示词)
  - [x] convertLLMOutputToPatchesWithContext: modify 模式使用引用图层 ID,animate 模式跳过动画字段
  - [x] 24 测试通过(EI01-EI12 + SP01-SP06 + DC01-DC06)

- [x] Step 36.3: Multi-turn Conversation — 多轮对话管理 + 21 测试(含 36.4)
  - [x] ConversationRole = 'user' | 'director'
  - [x] ConversationMessage: id / role / timestamp / content / decision? / intent?
  - [x] ConversationSession: id / messages / appliedPatches / createdAt
  - [x] 会话管理: createConversation / addUserMessage / addDirectorMessage / clearConversation
  - [x] serializeConversation: 序列化历史为文本(`[用户] ...` / `[Director] ...` 格式)
  - [x] buildConversationSystemPrompt: 包含历史 + 上下文的系统提示词

- [x] Step 36.4: Timeline Auto-Generation — 从 LLM 输出自动生成时间轴
  - [x] extractAnimationParams: 从 LLM 输出提取 animateFrom / animateTo / duration
  - [x] createTrackFromAnimation: 生成 2 个关键帧的 TimelineTrack
  - [x] createTimelineFromAnimations: 生成完整 TimelineContent(取最大 duration 为总时长)
  - [x] generateTimelineFromLLM: 一步到位从 LLM 输出生成 Timeline
  - [x] converse 多轮对话主入口: 集成意图解析 + 上下文提示词 + LLM 调用 + Timeline 生成
  - [x] 21 测试通过(S01-S05 + H01-H04 + C01-C04 + T01-T08)

- [x] Step 36.5: Director Panel UI — AI Director 对话式面板
  - [x] ProTimelineDirectorPanel.vue: 模态面板(按钮触发 + 右侧固定定位)
  - [x] 对话区: 用户消息(右对齐,accent 背景) + Director 消息(左对齐,bg 背景)
  - [x] API 配置区: provider(openai/anthropic) + apiKey + model + baseUrl
  - [x] 输入区: textarea + 模式指示器(create/modify/animate/analyze)
  - [x] 消息 patches 预览(前 3 个) + 思考中指示器 + 统计栏(消息数 + 修改数)
  - [x] 使用 shallowRef 避免 JsonLiteral 递归类型在 vue-tsc SFC 中触发 TS2589
  - [x] 集成到 ProTimeline.vue
  - [x] --pf-* 设计令牌 + cubic-bezier(0.22, 1, 0.36, 1) 180ms 过渡

- [x] Step 36.6: 全量测试 + vue-tsc + 文档更新
  - [x] 2504/2504 vitest 测试通过(75 个测试文件)
  - [x] vue-tsc 零错误(17 个新增错误全部修复 + 7 个预存错误全部清理)
  - [x] 17 个错误修复: Opcode enum(非 string) / CompileHints(preferredProfile 非 preferPartialRecompile) / LLMProviderConfig(defaultModel 非 model) / currentIr(小写 r) / shallowRef(避免 TS2589) / 未使用导入清理

## 已完成（Step 37.1-37.6 WDL 声明式渲染 DSL）

- [x] Step 37.1: WDL Lexer/Tokenizer — 词法分析 + 47 测试
  - [x] TokenType 枚举(KEYWORD/IDENT/STRING/NUMBER/LBRACE/RBRACE/LBRACKET/RBRACKET/COLON/COMMA/SIZE/EOF)
  - [x] WDL_KEYWORDS 集合(scene/layer/effect/region/canvas/opcode/type/target/bounds/layers/blendMode/visible)
  - [x] Token 接口(type/value/line/column)
  - [x] Lexer 类: 单遍扫描,支持空白/单行注释(//)/多行注释(/* */)/字符串转义(\"\n\t)/数字(整数/浮点/负数/科学计数法)/尺寸字面量(1920x1080)/标识符和关键字/标点符号
  - [x] LexerError 含行号列号
  - [x] 47 测试通过(L01-L47)

- [x] Step 37.2: WDL Parser + AST — 递归下降语法分析 + 39 测试
  - [x] AST 节点: SceneNode / LayerNode / EffectNode / RegionNode / ParamNode / ValueNode
  - [x] ValueNode discriminated union: number / string / boolean / array / ident
  - [x] Parser 类: 递归下降,parseScene / parseCanvas / parseLayer / parseEffect / parseRegion / parseParamBlock / parseParam / parseValue / parseArray
  - [x] 参数间可选逗号(CSS-like 多行用换行,单行用逗号)
  - [x] ParseError 含行号列号
  - [x] 39 测试通过(P01-P39)

- [x] Step 37.3: WDL Compiler — AST → RenderIR + 29 测试
  - [x] OPCODE_MAP(opcode 名称 → Opcode enum 映射)
  - [x] BLEND_MODES 集合(normal/multiply/screen/overlay/add/subtract)
  - [x] compileLayer: opcode 必填校验 + params 收集(排除 opcode/blendMode/visible) + paramOwnership 标记 l2_user
  - [x] compileEffect: type 必填校验 + target/targetRegion 提取
  - [x] compileRegion: bounds 4 元素数组校验 + layers 字符串数组校验
  - [x] valueToJsonLiteral: ValueNode → JsonLiteral 转换(支持嵌套数组)
  - [x] compileSource: 一站式 Lexer → Parser → Compiler
  - [x] 29 测试通过(C01-C29)

- [x] Step 37.4: WDL Validator — 语义校验 + 26 测试
  - [x] ValidationReport(valid/errors/warnings) + ValidationMessage(severity/message/line/column)
  - [x] ID 唯一性校验(layer/effect/region 名称重复检测)
  - [x] 引用完整性校验(effect.target / region.layers 引用的 layer 是否存在)
  - [x] 必填字段校验(layer.opcode / effect.type / region.bounds + layers)
  - [x] 参数值校验(bounds 4 数字数组 / color 3-4 元素 / width/height 正数)
  - [x] 警告(无图层 / 无区域 / layers 空数组 / canvas 尺寸过大)
  - [x] 多错误收集(不遇到第一个就停止)
  - [x] validateSource: 解析错误转为校验报告
  - [x] 26 测试通过(V01-V26)

- [x] Step 37.5: WDL Editor UI — 代码编辑器面板
  - [x] ProTimelineWDLEditor.vue: 模态面板(按钮触发 + 居中定位)
  - [x] 编辑器区: 行号 + textarea + 自动校验开关 + 校验/编译按钮 + 应用到运行时按钮
  - [x] 右侧: 消息列表(点击跳转行号) + 编译预览(canvas/layers/effects/regions 摘要)
  - [x] 模板加载(星空夜景/纯色测试/空白场景) + 格式化 + 清空
  - [x] shallowRef 避免 RenderIR 递归类型 TS2589
  - [x] 集成到 ProTimeline.vue
  - [x] --pf-* 设计令牌 + cubic-bezier(0.22, 1, 0.36, 1) 180ms + JetBrains Mono 代码字体

- [x] Step 37.6: 全量测试 + vue-tsc + 文档更新
  - [x] 2645/2645 vitest 测试通过(79 个测试文件,+141 新增)
  - [x] vue-tsc 零错误(ValueNode discriminated union narrow / 未使用导入清理 / STARRY_TEMPLATE 声明顺序 / App.vue 预存错误清理)
  - [x] 文档更新(分阶段任务清单.md + 项目阶段划分.md)

## 已完成（Step 38.1 Monaco Editor 集成 + WDL 语法高亮）

- [x] Step 38.1: Monaco Editor 集成 + WDL 语法高亮(Monarch tokenizer)
  - [x] wdlMonarch.ts — Monarch tokenizer 定义(关键字/opcode/blendMode/布尔/数字/尺寸/字符串/注释着色)
  - [x] wdlLanguageConfig.ts — 语言配置(括号匹配/自动闭合/注释切换/缩进规则/代码折叠)
  - [x] wdlTheme.ts — pixelforge-dark 主题(对齐 --pf-* 设计令牌)
  - [x] wdlRegister.ts — 幂等语言注册(registerWDLLanguage)
  - [x] ProTimelineWDLEditor.vue — textarea 替换为 Monaco Editor(语法高亮/括号匹配/Ctrl+/注释切换/自动缩进)
  - [x] @guolao/vue-monaco-editor + monaco-editor 本地打包(loader.config 不走 CDN)
  - [x] 33 测试通过(M01-M33,结构 + 映射 + token class 校验)
  - [x] 2678/2678 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 38.2 WDL 自动补全）

- [x] Step 38.2: WDL 自动补全(关键字/图层 ID/opcode/参数名)
  - [x] wdlCompletion.ts — CompletionItemProvider(7 类补全:块关键字 / 参数关键字 / opcode 值 / blendMode 值 / 布尔值 / 图层 ID 引用 / effect type 值)
  - [x] analyzeCompletionContext — 上下文检测(block 类型 / afterColon / 已声明 ID 收集)
  - [x] generateCompletions — 纯函数补全生成(便于测试)
  - [x] registerWDLCompletion — Monaco 注册(triggerCharacters / 范围计算 / snippet 占位符)
  - [x] wdlRegister.ts 集成 registerWDLCompletion
  - [x] 34 测试通过(A/B/C/D/E/F/G/H/I/J 10 组)
  - [x] 2712/2712 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 38.3 Validator 错误内联提示）

- [x] Step 38.3: Validator 错误内联提示(标记到代码行)
  - [x] wdlDiagnostics.ts — ValidationReport → Monaco markers 转换
  - [x] messageToMarker / reportToMarkers / errorToMarker / validateSourceToMarkers 纯函数
  - [x] applyMarkersToModel / clearMarkersFromModel Monaco 集成
  - [x] ProTimelineWDLEditor.vue 集成(doValidate 时自动应用 markers,卸载时清除)
  - [x] 25 测试通过(D01-D25)
  - [x] 附带修复:wdlCompletion.ts 移除顶层 monaco-editor 值导入(改用参数传入)
  - [x] 2737/2737 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 38.4 WDL ↔ Graph 双向同步）

- [x] Step 38.4: WDL ↔ Graph 双向同步(文本 ↔ 节点图互转)
  - [x] wdlGraphSync.ts — wdlToGraph / wdlSourceToGraph / graphToWdl
  - [x] 映射:WDL layer → REGION 节点,effect → EFFECT 节点,region.layers → 链式 edges
  - [x] 自动追加 OUTPUT 节点,自动布局坐标
  - [x] Round-trip 一致性:WDL → Graph → WDL 保持语义
  - [x] 25 测试通过(W01-W13 / G01-G08 / R01-R04)
  - [x] 2762/2762 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 38.5 WDL 模板库）

- [x] Step 38.5: WDL 模板库(场景预设)
  - [x] wdlTemplates.ts — 9 个预设(自然 3 + 都市 2 + 抽象 2 + 极简 2)
  - [x] 查询:getTemplatesByCategory / getTemplateById / searchTemplates / getCategories
  - [x] validateAllTemplates 确保所有模板合法可编译
  - [x] 29 测试通过(T01-T29)
  - [x] 2791/2791 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 38.6 WDL ↔ ProTimeline 绑定）

- [x] Step 38.6: WDL ↔ ProTimeline 绑定(片段属性 → DSL 变量)
  - [x] wdlTimelineBinding.ts — 绑定注册表(Clip 属性 ↔ Layer 参数)
  - [x] 绑定 CRUD:createBindingRegistry / addBinding / removeBinding / getBindingsForClip / getBindingsForLayer
  - [x] 属性提取:getClipPropertyValue(7 个可绑定属性)
  - [x] 值转换:computeParamValue(scale + offset)
  - [x] 补丁计算:computePatches(活跃 Clip + 绑定 → 参数补丁)
  - [x] 属性元数据:BINDABLE_PROPERTIES(label/group/min/max)
  - [x] 34 测试通过(B01-B11 / P01-P06 / V01-V05 / C01-C07 / M01-M05)
  - [x] 2825/2825 vitest + vue-tsc 0 WDL 相关错误

## 已完成（Step 39.1 渲染性能 Profiler）

- [x] Step 39.1: 渲染性能 Profiler(帧时间/GPU ms/内存/统计/等级)
  - [x] renderProfiler.ts — 统一三套性能指标(PerformanceMetrics / SchedulerMetrics / GpuResourceMetrics)为单一 FrameSample
  - [x] PerformanceLevel 4 级判定(good/ok/warn/bad)对齐 60FPS 预算(16.67/25/33.33ms)
  - [x] FrameSampleRingBuffer 环形缓冲区(默认 240 帧容量,push/getAll/getRecent/getLatest/clear/容量)
  - [x] computeStats 统计计算(纯函数:avg/P50/P95/P99/min/max/overBudget/overallLevel,百分位线性插值)
  - [x] createFrameSample 从 PerformanceMetrics → FrameSample(字段提取 + overBudget + level)
  - [x] 格式化辅助:formatMs / formatBytes / formatFps
  - [x] 45 测试通过(L01-L06 / S01-S11 / R01-R13 / ST01-ST10 / F01-F03 / C01-C02)
  - [x] 2870/2870 vitest + vue-tsc 0 新增错误(7 个预存错误:App.vue/Timeline.vue,与 Step 39.1 无关)

## 已完成（Step 39.2 GPU 资源池化）

- [x] Step 39.2: GPU 资源池化(纹理/Buffer 对象池)
  - [x] bufferPool.ts — GPU Buffer 复用池(对齐 TexturePool 设计模式)
  - [x] size 分桶策略(bucketSize:向上取整到 16 的倍数,避免池碎片化)
  - [x] 复用匹配条件:bucketed size + usage 完全相同 且 inUse=false
  - [x] MAP_READ/MAP_WRITE 拒绝池化(isPoolableUsage,避免异步 mapAsync 冲突)
  - [x] LRU 淘汰策略(池满时淘汰最久未使用,默认 maxPoolSize=64)
  - [x] GPU 注入式创建(createGpuBuffer/destroyGpuBuffer 回调,不持有 device 引用)
  - [x] 统计指标:hitRate / totalHits / totalMisses / totalRejected / totalBytes
  - [x] ResourceManager 扩展:统一管理 TexturePool + BufferPool
  - [x] disposeNode 同时释放纹理和 buffer,getStats 合并两套统计
  - [x] 56 测试通过(U/L/R/RJ/LRU/ST/G/RM/E 九组)
  - [x] 2926/2926 vitest + vue-tsc 0 新增错误(7 个预存错误保持不变)
  - [x] 附带修复:runtime.test.ts 3 处旧 ResourceStats 字段名适配

## 已完成（Step 39.3 多 Pass 渲染管线）

- [x] Step 39.3: 多 Pass 渲染管线(离屏 RenderTarget + 后处理链)
  - [x] renderTarget.ts — 离屏渲染目标抽象
    - [x] RenderTargetDescriptor:{ width, height, format?, usage?, label? }
    - [x] DEFAULT_RT_FORMAT = 'rgba8unorm'(DM-5 强制),DEFAULT_RT_USAGE = COPY_DST | TEXTURE_BINDING | STORAGE_BINDING
    - [x] normalizeDescriptor / isCompatibleDescriptor / estimateRenderTargetBytes
    - [x] createRenderTarget / destroyRenderTarget / acquireRenderTarget / releaseRenderTarget 生命周期
    - [x] GPU 注入式创建/销毁(createGpuTexture / destroyGpuTexture 回调)
  - [x] postProcessChain.ts — 多 Pass 后处理链(ping-pong 调度)
    - [x] PostProcessPass:{ id, name, kind('compute'|'render'), enabled, params, shaderKey }
    - [x] Pass 管理:addPass / insertPass / removePass / getPass / setPassEnabled / updatePassParams / clear
    - [x] execute(input, output, executor) — ping-pong 纹理调度
    - [x] 短路优化:0 pass 不调用 executor,1 pass 直连 input→output
    - [x] 2+ pass ping-pong:偶数 pass 写 rtA,奇数 pass 写 rtB,最后写 output
    - [x] 中间 RT 管理:尺寸兼容则复用,不兼容则销毁重建(ensurePingPong)
    - [x] disabled pass 跳过(passTimings 标记 skipped=true)
    - [x] 统计:totalPasses / enabledPasses / intermediateRTCount / intermediateRTBytes / totalExecutions / totalShortCircuits
    - [x] makePass 便捷工厂函数
  - [x] multiPassPipeline.ts — 多 Pass 管线编排器(scene → post-process → present)
    - [x] ScenePassExecutor / PresentPassExecutor 回调注入(纯编排,不直接调用 GPU)
    - [x] execute(params):准备 RT → sceneExecutor → postProcessChain.execute → presentExecutor
    - [x] 跳过后处理快速路径:scene → present 直连(从 inputRT present)
    - [x] RT 复用:ensureInputRT / ensureOutputRT(尺寸兼容则复用)
    - [x] setEnablePostProcess 开关,setPostProcessChain 设置链
    - [x] 统计:totalFrames / totalPostProcessSkipped / avgFrameMs / postProcessSkipRate
    - [x] dispose() 销毁 RT + chain,resetStats() 清零统计但保留 chain
  - [x] 61 测试通过(U/L/PM/EX/RT/ST/PP/G 十组)
  - [x] 2987/2987 vitest + vue-tsc 0 新增错误(7 个预存错误保持不变)
  - [x] 附带修复:mock GPUTexture 缺少 createView(添加 createMockTexture 辅助函数)+ vi.fn 类型标注(修复 TS2493 tuple 类型错误)

## 已完成（Step 39.4 渲染签名缓存）

- [x] Step 39.4: 渲染签名缓存(三级缓存命中率)
  - [x] renderSignature.ts — 渲染签名生成 + L1 签名缓存
    - [x] FNV-1a 32-bit hash(与 compileCache / pixelSignature 同算法,8 位 hex)
    - [x] RenderSignatureInput:{ staticKey, structuralKey, dynamicKey, canvasSize }
    - [x] computeRenderSignature / createSignatureFromKeys
    - [x] SignatureCache 类(LRU 淘汰,默认 maxSize=64)
    - [x] query / insert / get / has / invalidate / clear / entries
    - [x] 统计:hits / misses / hitRate / evictions / size / maxSize
  - [x] renderCache.ts — 三级渲染缓存编排器(L1→L2→L3 逐级降级)
    - [x] 三级层级:L1 签名缓存 → L2 artifact 缓存 → L3 资源池
    - [x] query(input, frameIndex, l2Hit):查询 L1 + 记录 L2 统计
    - [x] CacheLevel 类型:'L1' | 'L2' | 'L3' | 'miss'
    - [x] levelDistribution:各层级命中计数
    - [x] overallHitRate = (L1 hits + L2-only hits) / totalQueries
    - [x] L2 统计外部注入:recordL2Hit / recordL2Miss(不侵入 compileCache.ts)
    - [x] L3 统计外部注入:setL3Stats(ResourceManager 转换,自动计算 hitRate)
    - [x] RenderCacheStats:{ l1, l2, l3, totalQueries, totalHits, overallHitRate, levelDistribution }
    - [x] clearL1 / resetStats / dispose
    - [x] 支持注入外部 SignatureCache
  - [x] 设计原则:纯统计编排,不直接管理 L2/L3,避免侵入已有模块
  - [x] 67 测试通过(S/L/LRU/ST/Q/D/HR/L2R/L3/RS/C/E 十二组)
  - [x] 3054/3054 vitest + vue-tsc 0 新增错误(7 个预存错误保持不变)

## 已完成（Step 40.1 设置面板）

- [x] Step 40.1: 设置面板(主题切换 + 用户偏好 + 持久化)
  - [x] preferences/theme.ts — 主题预设与切换引擎
    - [x] DARK_TOKENS(复用 App.vue :root 21 token)+ LIGHT_TOKENS(新增浅色预设)
    - [x] ThemeMode = 'dark' | 'light' | 'auto',ResolvedTheme = 'dark' | 'light'
    - [x] getSystemTheme / resolveTheme / getThemeTokens / applyThemeTokens / applyTheme
    - [x] watchSystemTheme(监听 prefers-color-scheme 变化,auto 模式自动跟随)
    - [x] SSR 安全(typeof window / typeof document 检查)
  - [x] preferences/settingsStore.ts — 用户偏好 Pinia Store
    - [x] State:theme / language / autosaveIntervalMs / defaultCanvasWidth/Height / showPerformanceMonitor / restoreOnStartup
    - [x] Getters:resolvedTheme / isDark / isAutoTheme / autosaveIntervalSeconds
    - [x] Actions:setTheme / toggleTheme / setAutosaveInterval / setDefaultCanvasSize / setLanguage / setShowPerformanceMonitor / setRestoreOnStartup / resetToDefaults / init / dispose
    - [x] localStorage 持久化(键名 'pixelforge:settings',深度 watch flush:'sync')
    - [x] auto 模式启动系统主题监听
  - [x] components/editor/SettingsDialog.vue — 设置面板模态弹窗
    - [x] 4 个 Tab:外观 / 编辑器 / 自动保存 / 关于
    - [x] iOS 风格动画(180ms cubic-bezier(0.22, 1, 0.36, 1))
    - [x] Toggle 开关(iOS 风格)+ 滑块 + 画布预设按钮组
    - [x] 遵循项目设计语言(JetBrains Mono / --pf-* CSS 变量)
    - [x] Esc 关闭 + 背景点击关闭
  - [x] App.vue 集成:纠正 ⚙ 按钮误绑 + init/dispose 生命周期
  - [x] 66 测试通过(P/R/GT/AT/AP/W/I/T/A/C/M/LS/RS/G 十五组)
  - [x] 3120/3120 vitest + vue-tsc 0 新增错误(7 个预存错误保持不变)

## 已完成（Step 40.2 快捷键体系）

- [x] Step 40.2: 快捷键体系(CommandRegistry + CommandPalette)
  - [x] composables/commandRegistry.ts — 命令注册中心 + 快捷键匹配引擎(纯 TS,不依赖 Vue/DOM)
    - Command 接口(id/name/category/shortcut/altShortcut/execute/activeWhenEditing/enabled)
    - 6 大 CommandCategory:playback / history / project / editor / view / settings
    - eventToShortcut(event):KeyboardEvent → "mod+z" / "shift+mod+z" / "space" 字符串
    - 修饰键顺序 shift + alt + mod + key(对齐 mac ⇧⌥⌘ 显示)
    - formatShortcut(shortcut, platform):mac(⌘⇧⌥) / win(Ctrl+Shift+Alt)可读格式
    - isEditableTarget(target):焦点守卫(input/textarea/select/contenteditable)
    - CommandRegistry 类:register/unregister/get/list/listByCategory/search/execute/matchShortcut/setEnabled/rebindShortcut
    - 快捷键索引(shortcut→commandId Map),覆盖注册自动清理旧索引
    - commandRegistry 单例 + registerDefaultCommands(actions)注入 7 个默认命令
  - [x] composables/useCommandShortcuts.ts — 基于 CommandRegistry 的全局快捷键 composable
    - window keydown 监听 → matchShortcut → 焦点守卫 → execute
    - 替换原 useKeyboardShortcuts.ts(已删除),功能等价但完全数据驱动
  - [x] components/editor/CommandPalette.vue — Ctrl+K 命令面板(模态弹窗)
    - 搜索框(自动聚焦,实时过滤 name/id/description)
    - 按 category 分组(6 大类标签:播放控制/历史记录/编辑器/视图/项目/设置)
    - 键盘导航:ArrowUp/Down 选择 + Enter 执行 + Esc/背景点击关闭
    - 快捷键徽章(formatShortcut 跨平台格式化)
    - iOS 风格动画(180ms cubic-bezier):背景淡入 + 面板 scale+translateY
  - [x] App.vue 集成:
    - registerDefaultCommands 注入 7 个默认命令(对齐原 useKeyboardShortcuts)
    - 注册 view.commandPalette 命令(shortcut='mod+k',activeWhenEditing=true)
    - 替换 useKeyboardShortcuts() → useCommandShortcuts()
    - 挂载 CommandPalette 组件
  - [x] 66 测试通过(E01-E13 / F01-F09 / ET01-ET07[MockHTMLElement 注入] / R01-R08 / SB01-SB11 / EX01-EX04 / Q01-Q09 / DF01-DF05)
  - [x] 3186/3186 vitest + vue-tsc 0 新增错误(7 个预存错误保持不变)

## 待规划（Step 40.3+）

- Step 40.3: 项目导入导出增强(Tauri 原生文件 IO)
- Scene Graph(3D 场景树)大规模工程化
- 多人协作 / 云端同步
- 移动端适配
